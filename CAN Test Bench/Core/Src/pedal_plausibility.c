// This is the file where the functions for pedal plausibility checks will be

// use adc watchdog to trigger interrupts if values are above threshold perhaps

// use timers and start checking if both are high 

// if timer goes above 100 ms then disable motor controller input

// use interrupt if value of either pedal goes down to zero
